"""e5 — toy-scale composite AR + diffusion training (T0 from
`/Users/eren/.claude/plans/sfumato-vision-aligned-rosy-jellyfish.md`).

Stops the inference-bandaging arc of e1-e4 and tests the original Sfumato
vision (joint AR + diffusion training) at the smallest scale where it
could conceivably show signal: a 60M transformer with two output heads,
trained jointly on GSM8K-train, compared against parameter-matched
separated baselines.
"""
